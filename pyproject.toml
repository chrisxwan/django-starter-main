[tool.black]
exclude = '''
/(
  | migrations
  | eggs
  | git
  | hg
  | mypy_cache
  | tox
  | venv
  | _build
  | buck-out
  | build
  | dist
)/
'''

[tool.isort]
skip_glob = ["*/migrations/*.py"]
profile = "black"
multi_line_output = 3